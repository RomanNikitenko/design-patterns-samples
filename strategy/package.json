{
  "name": "strategy",
  "version": "0.0.1",
  "description": "Sample for the Strategy pattern",
  "private": true,
  "scripts": {
    "compile": "tsc",
    "test": "clear && npm install && npm run compile && node lib/index.js"
  },
  "devDependencies": {
    "@types/node": "^20.14.8",
    "typescript": "~5.7.3"
  }
}
