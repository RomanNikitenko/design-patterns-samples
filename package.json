{
  "name": "design-patterns-samples",
  "version": "1.0.0",
  "author": "Roman Nikitenko",
  "description": "Design patterns samples",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RomanNikitenko/design-patterns-samples.git"
  },
  "scripts": {
    "compile": "tsc",
    "adapter": "clear && cd adapter && npm install && npm run compile && node lib/index.js",
    "observer": "clear && cd observer && npm install && npm run compile && node lib/index.js",
    "strategy": "clear && cd strategy && npm install && npm run compile && node lib/index.js"
  }
}
